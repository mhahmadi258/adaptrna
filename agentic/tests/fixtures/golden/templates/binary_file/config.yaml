# rendered by adaptrna template v3 from spec.json
task: binary_file

head:
  hidden_dim: 32

data:
  root: "/home/user/data/example.csv"
  val_root: "/home/user/data/example_val.csv"
  batch_size: 32
  num_workers: 0

lora:
  r: 16
  alpha: 32
  dropout: 0.05
  layer_stride: 3

optim:
  name: adamw
  lr: 3.0e-4

trainer:
  max_epochs: 1
  precision: bf16-mixed
  gradient_clip_val: 1.0
